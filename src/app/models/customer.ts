export interface Customer {
  code: string;
  store: string;
  name: string;
  address: string;
  district: string;
  postalCode: string;
  cgc: string;
  phone: string;
  State: string;
  email: string;
  limit: string;
  usedLimit: string;
  lastShop: string;
  codeId: string
}
